import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const WorkoutStackNavigator = () => {
  return (
    <View>
      <Text>WorkoutStackNavigator</Text>
    </View>
  )
}

export default WorkoutStackNavigator

const styles = StyleSheet.create({})